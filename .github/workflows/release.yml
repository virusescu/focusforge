name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify Telegram build started
        shell: bash
        run: |
          TIME=$(date -u +"%Y-%m-%d %H:%M UTC")
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode "text=FocusForge ${{ github.ref_name }} build started at $TIME"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm install

      - name: Prepare signing key
        shell: pwsh
        run: |
          $key = @"
          ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          "@
          $keyPath = Join-Path $env:RUNNER_TEMP "focusforge.key"
          Set-Content -Path $keyPath -Value $key -NoNewline
          "TAURI_SIGNING_PRIVATE_KEY=$keyPath" | Out-File -FilePath $env:GITHUB_ENV -Append
          Write-Host "Signing key prepared at $keyPath"

      - name: Create GitHub Release
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --repo virusescu/focusforge \
            --title "FocusForge ${{ github.ref_name }}" \
            --notes "Release ${{ github.ref_name }}" || echo "Release already exists"

      - name: Get release ID
        id: get_release
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ID=$(gh release view "${{ github.ref_name }}" --repo virusescu/focusforge --json id --jq '.id')
          echo "release_id=$ID" >> $GITHUB_OUTPUT

      - name: Build and release
        id: tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ env.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          VITE_GOOGLE_CLIENT_ID: ${{ secrets.VITE_GOOGLE_CLIENT_ID }}
          VITE_GOOGLE_CLIENT_SECRET: ${{ secrets.VITE_GOOGLE_CLIENT_SECRET }}
        with:
          tagName: ${{ github.ref_name }}
          releaseId: ${{ steps.get_release.outputs.release_id }}
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true

      - name: List artifacts
        shell: pwsh
        run: |
          Get-ChildItem -Path src-tauri/target/release/bundle -Recurse

      - name: Upload updater artifacts
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $tag = "${{ github.ref_name }}"
          $repo = "virusescu/focusforge"
          $bundleDir = "src-tauri/target/release/bundle/nsis"

          Write-Host "Searching for updater artifacts in $bundleDir"
          $nsisZip = Get-ChildItem -Path $bundleDir -Filter "*-setup.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
          $nsisSig = Get-ChildItem -Path $bundleDir -Filter "*-setup.exe.sig" -ErrorAction SilentlyContinue | Select-Object -First 1

          if (-not $nsisZip -or -not $nsisSig) {
            Write-Host "Trying parent directory..."
            $nsisZip = Get-ChildItem -Path "src-tauri/target/release/bundle" -Filter "*-setup.exe" -Recurse | Select-Object -First 1
            $nsisSig = Get-ChildItem -Path "src-tauri/target/release/bundle" -Filter "*-setup.exe.sig" -Recurse | Select-Object -First 1
          }

          if (-not $nsisZip -or -not $nsisSig) {
            Write-Error "Could not find -setup.exe or -setup.exe.sig"
            exit 1
          }

          Write-Host "Uploading $($nsisZip.FullName) and $($nsisSig.FullName)"
          gh release upload $tag $nsisZip.FullName $nsisSig.FullName --repo $repo --clobber

      - name: Generate update.json
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart('v')
          $repo = "virusescu/focusforge"
          $tag = "${{ github.ref_name }}"
          $bundleDir = "src-tauri/target/release/bundle/nsis"

          $nsisZip = Get-ChildItem -Path $bundleDir -Filter "*-setup.exe" -ErrorAction SilentlyContinue | Select-Object -First 1
          $nsisSig = Get-ChildItem -Path $bundleDir -Filter "*-setup.exe.sig" -ErrorAction SilentlyContinue | Select-Object -First 1

          if (-not $nsisZip -or -not $nsisSig) {
            $nsisZip = Get-ChildItem -Path "src-tauri/target/release/bundle" -Filter "*-setup.exe" -Recurse | Select-Object -First 1
            $nsisSig = Get-ChildItem -Path "src-tauri/target/release/bundle" -Filter "*-setup.exe.sig" -Recurse | Select-Object -First 1
          }

          $sigContent = (Get-Content $nsisSig.FullName -Raw).Trim()
          $zipUrl = "https://github.com/$repo/releases/download/$tag/$($nsisZip.Name)"
          $pubDate = (Get-Date -Format "yyyy-MM-ddTHH:mm:ssZ")

          $updateJson = @{
            version  = $version
            notes    = "Release v$version"
            pub_date = $pubDate
            platforms = @{
              "windows-x86_64" = @{
                url       = $zipUrl
                signature = $sigContent
              }
            }
          } | ConvertTo-Json -Depth 5

          Set-Content -Path "update.json" -Value $updateJson -NoNewline
          Write-Host "Generated update.json for v$version"

      - name: Commit update.json
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Force checkout master and pull to make sure we are not on a detached head
          # and that we have the absolute latest if the user pushed during build
          git fetch origin master
          git checkout master
          git pull origin master
          
          # Add and commit the new update.json (which was generated in previous step)
          git add update.json
          git commit -m "chore: update update.json for ${{ github.ref_name }}"
          git push origin master

      - name: Notify Telegram success
        if: success()
        shell: bash
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode "text=FocusForge ${{ github.ref_name }} released successfully"

      - name: Notify Telegram failure
        if: failure()
        shell: bash
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode "text=FocusForge ${{ github.ref_name }} build failed"
