# Custom Sounds

Drop replacement audio files here. Supported formats: MP3, OGG, WAV.

| File | Plays when |
|------|-----------|
| `objective-complete.mp3` | An objective is neutralized |

If the file is missing or fails to load, the app falls back to the synthesized sound.
